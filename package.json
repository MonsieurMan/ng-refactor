{
  "name": "ng-refactor",
  "version": "0.0.1",
  "description": "Refactorings for angular in vscode",
  "repository": "https://gitlab.com/MonsieurMan/ng-refactor.git",
  "author": "MrMan",
  "license": "MIT",
  "publisher": "ng-refactor",
  "engines": {
    "vscode": "^1.22.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:ng-refactor.toggleInlineHTML",
    "onLanguage:typescript"
  ],
  "main": "./out/extension",
  "contributes": {
    "commands": [
      {
        "command": "ng-refactor.toggleInlineHTML",
        "category": "refactor",
        "title": "ng-refactor: Toggle inline HTML"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install"
  },
  "devDependencies": {
    "@types/node": "^10.5.0",
    "tslint": "^5.10.0",
    "typescript": "^2.1.4",
    "vscode": "^1.1.18"
  }
}
